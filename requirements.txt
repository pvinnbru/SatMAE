# SatMAE Finetuning Requirements
# Optimized for servers with limited space (6GB constraint)

# Core ML/DL libraries
torch==1.11.0
torchvision==0.12.0  
timm==0.3.2

# Data processing
numpy>=1.20.0,<1.22.0
pandas>=1.3.0,<1.4.0
pillow>=9.0.0,<10.0.0
rasterio>=1.1.0,<1.3.0

# Utilities
tqdm>=4.60.0
pyyaml>=6.0
wandb>=0.12.0

# Logging
tensorboard>=2.6.0

# Installation instructions:
# For CPU-only (recommended for 6GB constraint): pip install -r requirements.txt
# For GPU with CUDA 11.3: pip install torch==1.11.0+cu113 torchvision==0.12.0+cu113 -f https://download.pytorch.org/whl/torch_stable.html
# Then: pip install timm==0.3.2 numpy pandas pillow rasterio tqdm pyyaml wandb tensorboard
