# SatMAE GPU Requirements (CUDA 11.3)
# Install PyTorch with CUDA support first, then other packages

# Step 1: Install PyTorch with CUDA (run this first)
# pip install torch==1.11.0+cu113 torchvision==0.12.0+cu113 -f https://download.pytorch.org/whl/torch_stable.html

# Step 2: Install remaining packages
timm==0.3.2
numpy>=1.20.0,<1.22.0
pandas>=1.3.0,<1.4.0
pillow>=9.0.0,<10.0.0
rasterio>=1.1.0,<1.3.0
tqdm>=4.60.0
pyyaml>=6.0
wandb>=0.12.0
tensorboard>=2.6.0
